# Copy this file to .env and fill in your values.
# Never commit .env or real API keys to version control.

#==General Configuration==
App_Name="REZ ENHANCER"
APP_VERSION=0.1

#==LLM Configuration==
# At least one provider key is required (OpenAI or Google).
OPENAI_API_KEY=
GOOGLE_API_KEY=
GEMINI_LLM_MODEL=gemini-2.5-flash
OPENAI_LLM_MODEL=gpt-4o-mini
# Optional: custom OpenAI-compatible API base (e.g. Azure, local)
OPENAI_API_BASE=

#==Score Threshold==
# Minimum alignment score (e.g. 3) for enhancement suggestions.
SCORE_THRESHOLD=3
